interface:
  display_name: "Configure Google Tag Manager"
  short_description: "Operational client-side GTM configuration"
  default_prompt: "Use $configure-gtm to operationally implement my approved analytics tracking plan or explicit media brief in a dedicated client-side GTM workspace as a clean, well-organized, best-practice, consent-controlled saved setup; preserve analytics requirements exactly, apply basic CMP blocking by default, verify every saved object, and never publish."
